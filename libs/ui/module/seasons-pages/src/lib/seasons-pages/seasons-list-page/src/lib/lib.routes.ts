import { Route } from '@angular/router';
import { SeasonsListPageComponent } from './seasons-list-page/seasons-list-page.component';

export const seasonsListPageRoutes: Route[] = [
  { path: '', component: SeasonsListPageComponent },
];
